# Test environment - no Supabase credentials needed
# The app will automatically use mock data when these are not provided

# Optional: Set to true to force test mode even with credentials
NEXT_PUBLIC_TEST_MODE=true

# Optional: App configuration for test mode
NEXT_PUBLIC_APP_NAME="StudyTour Yellow Page (Test Mode)"